<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:orientation="horizontal"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:background="@color/katg_main_background">

    <ImageView
        android:id="@+id/show_coverimage"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_margin="10dp"
        android:background="@drawable/abc_menu_dropdown_panel_holo_dark"
        tools:background="@drawable/katg_show"/>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginRight="10dp">

        <TextView
            android:id="@+id/show_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/katg_show_title"
            android:textSize="22sp"
            android:textStyle="bold"
            tools:text="Keith and The Girl" />

        <TextView
            android:id="@+id/show_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:singleLine="false"
            android:paddingBottom="10dp"
            android:ellipsize="end"
            tools:text="Hosts Keith Malley and Chemda talk with comics about current events, pop culture, politics and even intimate details of their lives. They create a new hour-long show each weekday." />

    </LinearLayout>

</LinearLayout>